import styled from 'styled-components';

const H2 = styled.h2`
  font-family: 'Fredoka One';
  font-size: 3.5rem;
  margin: 1rem 0;
`;

const H3 = styled.h3`
  font-family: Raleway;
  font-size: 2.5rem;
  font-weight: 400;
  margin: 1rem 0;
`;

export default { H2, H3 };
